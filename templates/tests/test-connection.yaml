apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cache-node.fullname" . }}-test-connection"
  labels:
    {{- include "cache-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
  args: ['http://{{ include "cache-node.fullname" . }}:5071']
  restartPolicy: Never
