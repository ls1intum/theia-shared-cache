apiVersion: v2
name: theia-shared-cache
description: A custom Gradle Build Cache server with MinIO backend for Theia IDE deployments in Kubernetes

type: application

# Chart version - bump for breaking changes
version: 0.2.0

# Application version - matches the cache server version
appVersion: "0.1.0"

keywords:
  - gradle
  - cache
  - build-cache
  - theia
  - minio

home: https://github.com/kevingruber/theia-shared-cache

maintainers:
  - name: Kevin Gruber
